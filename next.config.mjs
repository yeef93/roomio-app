/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["img.icons8.com", "images.pexels.com", "www.pexels.com", "res.cloudinary.com"],
      },
};

export default nextConfig;
